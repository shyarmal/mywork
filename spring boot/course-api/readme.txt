This application uses derby database, which is in-built.
If configurations of spring boot application need to be changed, add/modify application.properties appropriately. 

To run:

1. Go to the project directory and run mvn clean install
2. Then run java -jar target/<cource-api jar file>
